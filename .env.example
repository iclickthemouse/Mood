# Do not put browser-exposed production secrets here.
# Current prototype accepts API keys in the in-app settings panel.
# Recommended next step: move model calls behind a server/API proxy before production.
